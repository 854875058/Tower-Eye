# Palantir 本体论 + 知识图谱落地方案

## 1. 文档目的

这份文档不是概念介绍，而是给当前 `data-chat-bot` 项目后续实施使用的执行方案。

目标是回答 6 个问题：

1. 我们为什么要做“Palantir 风格本体层 + 知识图谱层”
2. 这套东西应该落在当前项目的哪些模块里
3. 第一版对象、本体、关系到底定义成什么
4. 前后端分别怎么做
5. 每一阶段做完怎么验证
6. 如何判断这件事已经具备对甲方演示价值

这份文档默认面向两个场景：

- 内部研发推进：拆任务、排优先级、做里程碑
- 对外演示准备：保证最终形态像“业务对象驱动的平台”，而不是单纯的图谱展示页

---

## 2. 先说结论

这个项目里“Palantir 本体论 + 知识图谱”的核心，不是做一张图，而是引入一层统一业务对象模型，让查询、检索、联动、解释、推荐、处置动作都围绕对象展开。

换句话说：

- 错误方向：单独做一个“知识图谱页面”
- 正确方向：把本体层变成系统的中间层，让查询页、图谱页、工作台、处置建议都吃这一层

如果只做图：

- 甲方会觉得这是一个“可视化附件”
- 用户也只能看图，不能基于对象继续探索和操作

如果做成本体层：

- 查询结果里的“设备”“区域”“算法”“工单”都能变成对象入口
- 图谱不是孤立展示，而是服务于问答、联动、追问、处置
- 这才真正接近 Palantir Ontology 的价值

---

## 3. Palantir Ontology 在这个项目里的翻译

为了避免概念过空，这里把 Palantir Ontology 翻译成你这个项目可以直接实现的四层结构。

### 3.1 对象类型层 `Object Type`

定义系统里“什么算一个业务对象”。

当前项目建议至少定义这些对象类型：

1. `AlertEvent`
2. `Device`
3. `Region`
4. `Algorithm`
5. `MediaAsset`
6. `WarningOrder`
7. `ActionRecommendation`

后续可扩展：

8. `Person`
9. `Task`
10. `Subscription`
11. `Dataset`
12. `KnowledgeEntity`

### 3.2 对象实例层 `Object`

某一个具体对象，例如：

- 告警事件 `event_id=bc6ce2cc...`
- 设备 `device_name=厦门美驰工业高铁景观塔/视频监控设备2`
- 区域 `county_name=海沧区`
- 算法 `algorithm_name=车辆闯入监控`
- 工单 `warning_order_id=2.03E+25`

### 3.3 关系层 `Relation`

对象之间的业务关系，例如：

- 事件 `located_in` 区域
- 事件 `detected_on` 设备
- 事件 `detected_by` 算法
- 事件 `has_image` 图片
- 事件 `has_video` 视频
- 事件 `belongs_to_order` 工单
- 工单 `has_recommendation` 处置建议
- 设备 `deployed_in` 区域

### 3.4 动作层 `Action`

这层最关键，也是最容易被忽略的。

对象不仅能被展示，还能被操作。

例如点击 `Device` 对象后可以执行：

- 查看设备近 30 天告警趋势
- 查看设备关联图片/视频
- 查看设备高频告警类型
- 查看设备所在区域风险
- 生成巡检建议

只有这一层起来，系统才像 Palantir；否则只是“有图谱配置和图谱展示”。

---

## 4. 为什么这个项目现在适合融本体层

当前项目已经具备本体层落地的几个关键条件：

### 4.1 结构化对象已经存在

你现在已经有这些字段来源：

- 事件：`event_id`、`event_type`、`alarm_time`、`summary`
- 区域：`province_name`、`city_name`、`county_name`、`town_name`
- 设备：`device_name`、`device_code`、`channel_name`、`channel_code`
- 算法：`algorithm_name`、`algorithm_code`
- 工单：`warning_order_id`
- 媒体：`img_src_path`、`img_icon_path`、`video_path`

也就是说，第一版对象模型不是虚构出来的，而是直接可以从现有数据里投影出来。

### 4.2 查询页已经具备“对象驱动交互”的基础

当前查询页已经有：

- 结果表格
- 媒体预览
- 查看详情
- 图谱关联洞察
- 追问建议

这意味着：

- 不用从零造一整套 UI
- 只要把“字段”升级成“对象入口”，效果就会明显变化

### 4.3 已经有本体配置雏形

当前仓库已有：

- [tower_ontology_config.json](/E:/AI数据集项目/data-chat-bot/backend/data/tower_ontology_config.json)

这说明项目里已经接受“本体配置中心”这个方向，只是当前还偏展示型配置，没完全成为系统中间层。

### 4.4 图谱联动的业务价值很容易讲

对甲方来说，最有说服力的不是“我们能画图”，而是：

- 为什么这条告警会和那台设备、这个区域、那段视频、那套算法联起来
- 点开后能不能继续分析和处置

这正好是本体层最容易出价值的地方。

---

## 5. 第一版对象模型设计

以下是建议的 P0/P1 阶段对象模型，尽量做到：

- 足够小，能落地
- 足够全，能支撑演示

### 5.1 AlertEvent

定义：
告警事件是系统里最核心的业务对象，承担查询、关联、风险、处置入口。

关键字段：

- `event_id`
- `asset_id`
- `event_type`
- `alarm_time`
- `alarm_level`
- `alarm_source`
- `summary`
- `confidence_level`
- `warning_order_id`
- `algorithm_name`
- `device_name`
- `county_name`

建议展示字段：

- 事件类型
- 告警时间
- 置信度
- 设备
- 区域
- 工单
- 摘要

对象面板动作：

- 查看关联视频
- 查看关联图片
- 查看同设备告警
- 查看同区域趋势
- 生成处置建议

### 5.2 Device

定义：
设备是告警触发和持续分析的关键对象。

关键字段：

- `device_code`
- `device_name`
- `channel_code`
- `channel_name`
- `address`
- `county_name`
- `algorithm_name`

建议展示字段：

- 设备名
- 通道
- 所在区域
- 当前关联算法

对象动作：

- 查看设备近 30 天告警趋势
- 查看设备 Top 告警类型
- 查看设备关联视频
- 查看设备风险分数

### 5.3 Region

定义：
区域是聚合分析、风险排序、属地处置的关键对象。

关键字段：

- `province_name`
- `city_name`
- `county_name`
- `town_name`

对象动作：

- 查看区域告警趋势
- 查看区域高频设备
- 查看区域高频算法
- 查看区域风险排行

### 5.4 Algorithm

定义：
算法对象用于承载模型能力覆盖、命中情况、误报复盘。

关键字段：

- `algorithm_code`
- `algorithm_name`
- `warning_type_name`
- `app_scenarios`

对象动作：

- 查看算法命中分布
- 查看算法关联设备
- 查看算法误报样本
- 查看算法在哪些区域高频出现

### 5.5 MediaAsset

定义：
媒体对象用于将图片、视频、预览图、抽帧统一成可挂接证据。

关键字段：

- `asset_id`
- `media_type`
- `img_src_path`
- `img_icon_path`
- `video_path`
- `preview_path`

对象动作：

- 放大查看
- 查看关联事件
- 查看同工单证据
- 加入标注/训练样本池

### 5.6 WarningOrder

定义：
工单对象将事件和处置链路串起来。

关键字段：

- `warning_order_id`
- `order_status`
- `process_name`
- `flow_id`
- `time_limit`

对象动作：

- 查看工单关联事件
- 查看工单关联媒体
- 查看工单处置建议
- 查看工单负责人/流程

### 5.7 ActionRecommendation

定义：
建议对象承载智能分析后的下一步动作。

关键字段：

- `recommendation_id`
- `title`
- `reason`
- `priority`
- `action_type`

对象动作：

- 标记执行
- 发送工作台
- 加入处置队列

---

## 6. 第一版关系设计

建议先实现这些“足够有业务价值”的关系。

### 6.1 事件到区域

- `AlertEvent -> Region`
- 关系：`located_in`

意义：
区域聚合分析、区域风险展示、区域追问入口。

### 6.2 事件到设备

- `AlertEvent -> Device`
- 关系：`detected_on`

意义：
设备画像、通道追踪、连续告警分析。

### 6.3 事件到算法

- `AlertEvent -> Algorithm`
- 关系：`detected_by`

意义：
算法能力覆盖、误报归因、模型复盘。

### 6.4 事件到媒体

- `AlertEvent -> MediaAsset`
- 关系：`has_image`
- 关系：`has_video`

意义：
让事件对象成为证据入口，而不是让媒体路径散在表格字段里。

### 6.5 事件到工单

- `AlertEvent -> WarningOrder`
- 关系：`belongs_to_order`

意义：
把查询、证据、处置链路串起来。

### 6.6 工单到建议

- `WarningOrder -> ActionRecommendation`
- 关系：`has_recommendation`

意义：
从“分析结果”进入“动作层”。

### 6.7 设备到区域

- `Device -> Region`
- 关系：`deployed_in`

意义：
做设备-区域双向联动。

### 6.8 事件到事件

- `AlertEvent -> AlertEvent`
- 关系：`related_event`

生成规则可基于：

- 同设备
- 同区域
- 同算法
- 同工单
- 同时间窗口

意义：
这是图谱联动和追问建议里最容易出效果的关系。

---

## 7. 系统落地架构

建议不要第一版就上重型图数据库。

当前项目更适合：

`结构化主存储 + 本体配置层 + 图谱投影层 + 前端对象面板`

### 7.1 这一版怎么做

#### A. 主数据仍然留在当前数据库/表

当前已有：

- DuckDB 查询层
- SQLite 元数据层

不需要立刻推翻。

#### B. 新增本体配置层

继续强化：

- [tower_ontology_config.json](/E:/AI数据集项目/data-chat-bot/backend/data/tower_ontology_config.json)

把它从“演示配置”升级成：

- 对象类型配置
- 字段映射配置
- 关系映射配置
- 对象动作配置

#### C. 后端动态生成“局部图谱子图”

当用户查询后：

1. 查询得到结果行
2. 后端将结果行映射为对象
3. 根据本体配置生成关系
4. 返回局部图谱子图给前端

优点：

- 不需要先建全量图谱库
- 非常适合演示
- 查询结果天然就是图谱入口

#### D. 前端围绕对象联动，而不是围绕字段联动

例如：

- 点击设备名，不是拼字符串查询
- 而是打开 `Device` 对象面板

对象面板展示：

- 概览
- 关系
- 分析
- 动作

---

## 8. 前端落地路径

### 8.1 P0：对象点击入口

目标：
把字段变成对象入口

做法：

1. 查询结果表格中以下字段变成可点击：
   - `device_name`
   - `county_name`
   - `algorithm_name`
   - `warning_order_id`
2. 点击后打开右侧抽屉或侧边对象面板
3. 面板先展示基础信息和几个推荐动作

验证：

- 点击设备能打开对象面板
- 点击区域能打开对象面板
- 面板里能看到对象基础信息

### 8.2 P1：对象面板

对象面板建议结构：

1. `概览`
2. `关系`
3. `分析`
4. `动作`

#### 概览

展示对象核心字段。

#### 关系

展示与当前对象直接相关的事件、设备、区域、媒体、工单等。

#### 分析

展示趋势图、TopN、分布图。

#### 动作

提供：

- 继续追问
- 查看详情
- 生成建议
- 加入样本池

验证：

- 设备面板能显示关联事件列表
- 区域面板能显示趋势图
- 算法面板能显示分布

### 8.3 P2：图谱视图

不是先做，但后面要补。

要求：

- 不是全量大图
- 而是基于当前对象/当前查询结果生成局部图

支持：

- 点击节点联动对象面板
- 点击关系高亮来源记录
- 支持从图谱回到查询结果

验证：

- 当前查询能生成一张局部对象图
- 点图谱节点后对象面板切换

---

## 9. 后端落地路径

### 9.1 P0：本体配置结构升级

当前配置文件建议升级出这些字段：

#### 对象类型

- `type`
- `label`
- `description`
- `key_fields`
- `display_fields`
- `searchable_fields`
- `action_defs`

#### 关系类型

- `type`
- `label`
- `source_type`
- `target_type`
- `match_rule`
- `strength`
- `enabled`

验证：

- 配置文件能表达对象、关系、动作三类信息

### 9.2 P1：对象投影服务

新增服务，例如：

- `ontology_projection.py`

职责：

1. 输入查询结果行
2. 输出对象列表
3. 输出关系列表
4. 输出对象摘要

返回结构建议：

```json
{
  "objects": [],
  "relations": [],
  "object_panels": {},
  "graph_stats": {}
}
```

验证：

- 给 20 条事件结果，能产出对象和关系
- 同设备/同区域/同算法关系能正确生成

### 9.3 P2：对象详情接口

新增接口，例如：

- `GET /api/v1/ontology/object/{type}/{id}`
- `POST /api/v1/ontology/subgraph`

作用：

- 按对象获取详情
- 获取某对象周围的局部子图

验证：

- 调设备接口能返回设备基本信息、关联事件、趋势
- 调区域接口能返回区域画像

---

## 10. 查询页如何融本体层

这是最有演示价值的一层。

### 当前问题

现在查询页虽然已经有：

- 图谱关联洞察
- 追问建议
- 查看详情

但它本质上还是：

- 基于字段和结果行在做展示

### 目标状态

查询页应该变成：

1. 用户提问
2. 系统返回结果
3. 结果中的关键字段提升为对象入口
4. 图谱洞察说明这些对象为什么相关
5. 追问建议围绕对象继续生成

### 具体做法

#### 第一层：对象入口

在结果表格和详情区中，把：

- 设备名
- 区域名
- 算法名
- 工单号

做成统一对象链接组件。

#### 第二层：对象面板

点击对象后，不直接替换整页，而是：

- 右侧抽屉打开对象详情

#### 第三层：对象驱动追问

追问建议不再只是：

- “查看更多”

而是：

- 查看该设备近 30 天趋势
- 查看该区域高频告警
- 查看该算法命中分布

验证：

- 用户点结果里的设备名，能打开 `Device` 面板
- 面板里能继续点动作去发起下一轮查询

---

## 11. 本体配置页怎么做

你之前说“本体论应该是搞一个配置页面”，这个判断是对的。

### 页面目标

不是给技术人员改 JSON，而是给系统定义：

- 什么是对象
- 什么是关系
- 什么字段映射到什么对象
- 点击对象后展示什么、能做什么

### 页面建议模块

1. `对象类型管理`
2. `字段映射管理`
3. `关系定义管理`
4. `动作定义管理`
5. `预览与调试`

### 验证

- 页面修改配置后，查询页对象投影立即变化
- 新增关系定义后，图谱洞察变化

---

## 12. 分阶段里程碑

### Phase 1：本体层跑通

范围：

- 本体配置升级
- 对象类型和关系定义
- 查询结果投影子图
- 查询页对象点击入口

验收标准：

- 查询页里至少四类对象可点击
- 后端能根据查询结果生成局部图谱

### Phase 2：对象面板跑通

范围：

- 对象面板
- 对象详情接口
- 对象驱动追问
- 区域/设备/算法面板基础分析

验收标准：

- 点击对象后可查看概览/关系/分析/动作
- 至少三类对象具备二次分析能力

### Phase 3：动作层跑通

范围：

- 建议对象
- 工单/样本/标注联动
- 处置动作回流

验收标准：

- 图谱不再只是看图，而是可以驱动动作

---

## 13. 怎么验证“像 Palantir”

不能只看有没有图谱页，要按下面标准验。

### 13.1 不合格状态

- 只有一张图
- 点节点没动作
- 查询页和图谱页是两张皮
- 图谱只服务展示，不服务问答和联动

### 13.2 合格状态

- 查询结果中的关键字段已对象化
- 对象可点开详情面板
- 面板可继续追问
- 图谱可展示对象之间关系
- 建议动作与工单/样本回流可联动

### 13.3 优秀状态

- 查询、图谱、面板、动作统一围绕对象工作
- 用户感觉自己不是在查表，而是在围绕业务对象探索

---

## 14. 风险与规避

### 风险 1：做成“图谱展示附件”

规避：

- 优先做对象面板和对象动作
- 不优先做孤立大图页

### 风险 2：对象定义过大，第一版做不动

规避：

- 第一版只做 7 个核心对象

### 风险 3：前端做成一堆新页面，割裂现有查询体验

规避：

- 先从查询页对象面板开始
- 图谱页作为补充，不作为唯一入口

### 风险 4：后端引入重型图库导致复杂度过高

规避：

- 第一版使用图谱投影层，不急着引入重型图数据库

---

## 15. 推荐实施顺序

如果明天开始正式做，建议顺序是：

1. 升级 `tower_ontology_config.json`
2. 做对象投影服务
3. 查询页结果字段对象化
4. 做对象面板
5. 做区域/设备/算法三类对象的基础分析
6. 做图谱视图
7. 做动作层

---

## 16. 当前建议的下一步任务

如果按照这份文档落地，最优先的 3 个任务是：

### Task 1

补一版“本体配置中心”的对象/关系/动作配置能力。

### Task 2

新增后端对象投影服务，让查询结果可以返回：

- 对象
- 关系
- 局部子图
- 对象面板摘要

### Task 3

在查询页里把 `device_name / county_name / algorithm_name / warning_order_id` 做成统一对象入口，并接对象面板。

---

## 17. 这份文档如何使用

后续推进时建议这样用：

1. 每次只从这份文档里挑一个阶段做
2. 每做完一项就在文档对应位置补状态
3. 每次验收按“验证方式/验收标准”逐条核对
4. 如果对象模型或关系模型调整，优先更新这份文档

这样它就不是一次性的想法记录，而是你们后续推进 Palantir 本体层的主线文档。
