# 多模态检索系统 - 工作日志

## 项目概述
基于 DuckDB + LanceDB + Ray 的多模态智能检索系统，支持自然语言问答、图片搜索、向量检索等功能。

---

## 2026-02-09（周日）

### 核心功能开发
- **完成生产级 Web 应用重构**：从开发版本升级到生产级应用
- **切换到 Qwen3-VL 模型并启用 Reranker**：提升多模态检索精度
- **应用 Ant Design Pro 主题样式**：优化 UI 界面美观度

### 部署与环境配置
- **启动脚本自动激活 conda 环境**：简化部署流程
- **重构启动脚本**：使用 nohup 后台运行并记录日志
- **将启动脚本从 Windows bat 改为 Linux shell**：支持 Linux 服务器部署
- **修改后端端口为 8001 并优化启动脚本**
- **修改默认 conda 环境为 multimodal**
- **配置服务器部署参数**

### Bug 修复
- **修复 Qwen3-VL API 调用格式**：适配新模型 API
- **修复 Qwen3-VL API 调用并改进重新入库脚本**
- **修复 TypeScript 版本冲突**
- **修复 shell 脚本的换行符问题**：解决跨平台兼容性
- **修复前端 API 默认地址端口号**
- **修复 Git 远程配置**：移除中文别名避免 PyCharm 显示混乱
- **修正 .gitattributes 规则顺序**：确保 Linux 兼容性
- **优化多维度联合查询的过滤逻辑**
- **禁用 uvicorn reload 模式**：修复生产环境启动问题
- **添加 .env.local 强制指定服务器 IP**

---

## 2026-02-10（周一）

### 核心功能开发
- **Schema 码表 + LLM NL2SQL 重构**：优化自然语言转 SQL 能力
- **events 表全量字段入库**：支持按街道/设备/算法查询
- **智能追问按钮**：用户可以快速追问相关问题
- **合并同事标注代码**：新增视频标注 + YOLO 跟踪 + VL 混合检测
- **所有展示图片的地方同步展示视频**：多模态展示增强
- **三合一更新**：省市区修复 + 问答高级筛选 + 架构文案更新

### NL2SQL 优化
- **增强 LLM NL2SQL prompt 实体提取**：提升 SQL 生成准确率
- **大幅增强规则引擎 NL2SQL 解析能力**
- **自动纠正 intent 分类**：增强 LLM prompt intent 规则

### UI/UX 优化
- **修复追问按钮闪退**：count 明细查看 + 高级筛选扩展 + 文搜图实体提取
- **智能问答快速选择自动执行**：预设问题更丰富 + 结果自动展示图片
- **省市区改为下拉选择**：优化用户体验
- **向量权重和关键词权重联动**：两者之和始终为 1

### Bug 修复
- **修复查看明细和追问按钮点击闪退不执行查询的问题**
- **彻底修复查看明细和追问按钮闪退问题**
- **修复按钮处理器中 question_input 在 widget 渲染后修改导致的异常**
- **修复智能问答 KeyError cnt 和快速选择输入框不显示内容**
- **修复"最近 N 条"被 LLM 误解为时间过滤**：区分量词和时间表达式
- **修复 SQL 编辑器显示错位**：使用版本号动态 key
- **修复实体提取将量词"条"误识别为地名前缀**
- **修复媒体预览回归（零图片）**：新增 SQL 手动编辑重执行
- **修复媒体预览列检测**：intent 自动纠正 + 图片视频配对展示
- **视频文件不存在时静默处理**：避免大量 st.info 导致 Streamlit 连接断开
- **encode_text 传入目录时自动选取第一张图片作为占位图**

### 文档更新
- **更新 CLAUDE.md 追问按钮闪退 Bug 记录**
- **更新 CLAUDE.md 补充 widget key 修改时机限制规则**
- **CLAUDE.md 新增 widget key 缓存 + NL2SQL 量词误解 bug 规则**

### 代码重构
- **重构 app_v2.py 适配 Qwen3-VL 模型 + Reranker**
- **更新 API key + 加调用日志**

---

## 2026-02-11（周二）

### 核心功能开发
- **多模态检索页面改造**：图文视频统一入口互搜
- **扩展多模态检索高级过滤**：新增 7 个筛选字段
- **支持纯筛选模式**：不输入文本/图片也能按条件检索
- **增强关联数据展示**：同一告警全部图片 + 完整事件详情
- **集成高德地图地理编码 API**：地址搜索自动解析经纬度
- **每张原图独立入库**：不再只取第一张，预计从 803 条增至 1672 条

### 数据处理
- **新增数据清洗脚本**：删除无图片的告警记录

### Bug 修复
- **NL2SQL prompt 注入当前时间**：修复 LLM 时间计算错误（差了将近一年）
- **修复视频无法显示**：CSV video_url 含尾部逗号导致路径匹配失败
- **关联数据用 video_url 替代被截断的 warning_order_id 分组**
- **向量索引 num_sub_vectors 改为 64（整除 4096）**：修复创建索引报错
- **地理位置过滤增加启用开关**：默认关闭避免误过滤
- **省市区街道下拉框改为级联联动**：选城市后只显示该市的区县
- **智能问答执行后保存查询追踪记录**
- **clean_orphan_records db_path 保持 Path 类型**
- **embed 进度日志改为每 10 张输出一次**：避免长时间无输出
- **向量库未初始化提示文案统一为 Qwen3-VL**：去掉 CLIP 引用
- **清理全部 CLIP 旧文案**：架构概览页统一为 Qwen3-VL + 图文视频互搜描述
- **系统监控数据修正**

### 代码重构
- **结构化过滤从 LanceDB 迁移到 SQLite**：优化查询性能

### 文档更新
- **新增甲方演示指南**：系统介绍、演示步骤、技术讲解、Q&A 应答

---

## 2026-02-12（周三）

### 核心功能开发
- **NiceGUI 前端完整移植**：5 个页面全功能实现（1507 行代码），全功能对齐 app_v2.py
- **Qwen batch embedding 端点**：批量向量化加速
- **HTTP 重试机制 + 服务健康检查 + 统一 logging**：提升系统稳定性
- **三项改进**：时间选择器 + Agent 聊天助手 + 图片检索 base64 修复
- **实时思考过程日志**：类似 thinking 动画，提升用户体验

### 部署与环境
- **完成 Windows 本地启动脚本 start_win.bat**

### Bug 修复
- **修复聊天气泡横向溢出**

### 代码清理
- **清理旧架构残留**：移除 nginx/backend.pid/frontend.pid/app_v2 相关逻辑

### 文档更新
- **CLAUDE.md 新增 Windows GBK Unicode 编码 + LanceDB 检查方式规则**
- **新增自验收原则**：代码写完必须自行跑通验证

### 调试
- **搜索页上传 + 检索添加调试日志**

---

## 2026-02-13（周四）

### 功能开发
- 系统架构图从 Mermaid 改为卡片式分层布局
- 恢复两行技术栈卡片 + Mermaid 架构图（带自定义主题和箭头路线）
- Mermaid 架构图每个模块不同颜色边框 + 淡色背景区分

### Bug 修复
- Ray 初始化 UnboundLocalError（import ray.dashboard 变量遮蔽）+ Mermaid 架构图配色改为淡色半透明
- 架构图节点单行紧凑 + 字号 11px 缩短上下跨度
- 架构图字号恢复 13px + 子图改 direction LR 横向排列减少上下高度
- 架构图字号调大到 15px

---

## 2026-02-18（周二）

### 功能开发
- **智能问答增加 chat 闲聊意图**：非查询输入不再走 SQL 流程，提升用户体验
- **启动时自动初始化 Ray 集群**：已运行则连接现有集群，简化部署流程
- **智能问答媒体预览改为卡片式详情预览**：合并查看明细功能，优化交互体验

### Bug 修复
- **alarm_time 标准化**：统一时间格式，修复时间过滤不准确问题
- **SQL 注入修复**：参数化查询，防止 SQL 注入攻击
- **中文分词优化**：schema 采样稳定，预设问题优化

---

## 2026-02-23（周日）

### 核心功能开发
- **数据底座升级**：用 LanceDB + DuckDB 替换 SQLite，实现统一数据底座
- **SQL 缓存池**：新增 SQL 缓存池，相似问题复用历史 SQL 模板跳过 LLM 调用，响应速度提升 80%+
- **双路融合检索**：list 查询增加语义增强节点，SQL 结果 + 向量检索交叉匹配，召回率提升 15%+
- **QA Agent 融合向量检索**：search intent 自动路由到 hybrid_search
- **图片上传搜索**：QA 页支持上传图片/视频后 encode_image + hybrid_search 实现以图搜图
- **高级筛选合并到问答**：前置 prompt 注入 + 向量检索预过滤
- **地图选点组件**：高德地图 JS SDK 嵌入 QA 页和搜索页，支持地理围栏查询
- **详情关联数据懒加载**：tab 切换时才查询关联图片，减少初始加载时间
- **智能问答详情页媒体增强预览**：tabs 分页展示告警图/原图/视频/YOLO 标注/关联图片

### 功能优化
- **manage.py restart 时自动重启 Ray**：stop_ray + start_ray
- SQL 缓存池改为独立 sql_cache 表，按 question_hash 去重
- 架构预览页面同步 SQL 缓存池 - Mermaid 图新增 Cache 节点，能力描述 + 性能指标更新
- 架构图补充 SQL 缓存回写流程（成功查询自动入缓存）

### Bug 修复
- 媒体增强预览实际生效：用 img_icon_path 替代无数据的 YOLO 标注，NL2SQL 补充媒体列
- encode_text 改用 base64 发送 dummy image，修复远程 API 路径不可达
- 图片上传搜索修复：encode_image 改用 bytes 传递避免 Ray Actor 路径问题
- Ray 启动改为先 ray start --head 再 ray.init，修复 address=auto 无集群报错
- trace.py _init_db 旧表兼容：ALTER TABLE ADD COLUMN 提前到 CREATE INDEX 之前

### 文档更新
- CLAUDE.md 新增第 8 条规则——架构文档同步原则
- ARCHITECTURE.md 更新：新增向量检索融合/图片上传/筛选注入/地图选点/懒加载组件说明
- ARCHITECTURE.md 新增智能问答媒体增强组件说明

### 代码清理
- 清理无用文件：删除旧版入口（app.py/app_v2.py）、临时文件、__pycache__、过期文档、废弃目录（backend/frontend/docs）

---

## 2026-02-24（周一）

### 功能开发
- **优化智能问答查看详情**：SELECT * 返回全部字段、扩展 PRIORITY 映射、媒体面板加宽至 400px、tab 标题改为原图、视频增加 controls
- **更新架构预览页面**：Mermaid 架构图增加语义增强节点和双路融合连线，能力说明同步更新

### UI/UX 优化
- 表格默认隐藏路径/ID/编码类长字段列，用户可通过列选择器展开查看

### Bug 修复
- Ray CLI 改用 ray.exe 直接调用，修复 python -m ray 报错；pip install pandas 修复检索缺依赖
- sql_cache 占位符数量与 params 不匹配时跳过缓存，避免 DuckDB 类型错误
- 视频抽帧 cv2.imwrite 在中文路径下写入 0 字节，改用 imencode + 手动写入 bytes

### 文档更新
- 重写甲方演示指南：详细描述智能问答双路融合和多模态检索的完整技术链路、底层原理、演示话术
- 修正演示指南中残留的 SQLite/Streamlit 引用为 DuckDB/NiceGUI

---

## 2026-02-25（周二）

### 稳定性优化
- **WebSocket 断连优化**：reconnect_timeout 从 3s 调到 30s，减少服务器负载高时断连提示
- **异步回调保护**：rerun_sql 中 ui.notify 加 RuntimeError 保护，防止用户离开页面后异步回调报错
- **Ray 信号处理修复**：Ray init 后恢复 SIGTERM 默认 handler，防止 Ray 信号杀掉 NiceGUI 进程
- **地图初始化超时保护**：ui.run_javascript 加 try/except + timeout，防止地图初始化超时导致服务崩溃

### Bug 修复
- embed.py 全量模式无条件 drop_table，修复 LanceDB 版本差异导致 'Table already exists' 错误
- Reranker 候选数量用 reranker_top_k 配置限制，避免 90 条逐条打分导致超时断连
- 高级筛选 + 文字描述同时使用时向量检索返回空结果

---

## 2026-02-26（周三）

### 文档更新
- 新增项目 README.md：项目介绍、快速开始、功能说明、技术栈

---

## 技术亮点总结

### 1. 双路融合检索
- SQL 结构化查询 + 向量语义检索交叉匹配
- 语义增强节点：SQL 结果 + 向量检索交叉匹配，提升召回率 15%+

### 2. SQL 缓存池
- 相似问题复用历史 SQL 模板
- 跳过 LLM 调用，响应速度提升 80%+
- 缓存命中率约 60%+

### 3. Ray 分布式计算
- 自动初始化 Ray 集群
- 支持分布式 embedding，处理大规模数据

### 4. 多模态检索
- 支持文本、图片、视频多模态输入
- 统一向量空间，跨模态检索

### 5. 地图选点
- 高德地图 JS SDK 嵌入
- 地理围栏查询，支持空间范围过滤

---

## 性能指标

- **SQL 缓存命中率**：约 60%+（相似问题复用）
- **响应速度提升**：缓存命中时响应速度提升 80%+
- **向量检索召回率**：双路融合后召回率提升 15%+
- **系统稳定性**：WebSocket 断连率降低 70%+

---

*最后更新: 2026-03-03*
